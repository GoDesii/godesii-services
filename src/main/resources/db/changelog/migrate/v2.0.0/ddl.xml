<?xml version="1.0" encoding="UTF-8"?>
<databaseChangeLog
        xmlns="http://www.liquibase.org/xml/ns/dbchangelog"
        xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
        xsi:schemaLocation="http://www.liquibase.org/xml/ns/dbchangelog
                            http://www.liquibase.org/xml/ns/dbchangelog/dbchangelog-4.25.xsd">

    <!-- ============================================================ -->
    <!--           V1.0.0 — SCHEMA MIGRATION (DDL CHANGES)            -->
    <!-- ============================================================ -->
    <!-- Place ALTER TABLE, ADD COLUMN, DROP COLUMN, RENAME,
         CREATE TABLE (new entities), ADD/DROP INDEX, ADD/DROP FK
         changesets here for the v1.0.0 release cycle.                -->

    <!--
    <changeSet id="v1.0.0-ddl-001" author="admin">
        <comment>Example: Add a new column to an existing table</comment>
        <addColumn tableName="user">
            <column name="profile_image_url" type="VARCHAR(500)"/>
        </addColumn>
    </changeSet>

    <changeSet id="v1.0.0-ddl-002" author="admin">
        <comment>Example: Create a new table for a new feature</comment>
        <createTable tableName="user_preferences">
            <column name="id" type="BIGINT" autoIncrement="true">
                <constraints primaryKey="true" nullable="false"/>
            </column>
            <column name="user_id" type="BIGINT">
                <constraints nullable="false"/>
            </column>
            <column name="preference_key" type="VARCHAR(100)">
                <constraints nullable="false"/>
            </column>
            <column name="preference_value" type="VARCHAR(500)"/>
        </createTable>
        <addForeignKeyConstraint
                baseTableName="user_preferences"
                baseColumnNames="user_id"
                constraintName="fk_user_preferences_user"
                referencedTableName="user"
                referencedColumnNames="id"/>
    </changeSet>

    <changeSet id="v1.0.0-ddl-003" author="admin">
        <comment>Example: Add index for new column</comment>
        <createIndex indexName="idx_user_profile_image" tableName="user">
            <column name="profile_image_url"/>
        </createIndex>
    </changeSet>
    -->

</databaseChangeLog>
