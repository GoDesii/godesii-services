name: 🚀 Deploy Spring Boot App to AWS EC2

on:
  push:
    branches:
      - feature/GODESI-1  

jobs:
  build-deploy:
    runs-on: ubuntu-latest

    steps:
      # -------------------------------
      # 1️⃣ Checkout source code
      # -------------------------------
      - name: Checkout code
        uses: actions/checkout@v4

      # -------------------------------
      # 2️⃣ Set up Java environment
      # -------------------------------
      - name: Set up JDK 17
        uses: actions/setup-java@v4
        with:
          distribution: 'temurin'
          java-version: '17'
          cache: 'gradle'

      # -------------------------------
      # 3️⃣ Grant permission to Gradle wrapper
      # -------------------------------
      - name: Make gradlew executable
        run: chmod +x gradlew

      # -------------------------------
      # 4️⃣ Build project with Gradle
      # -------------------------------
      - name: Build JAR
        run: ./gradlew clean build -x test

      # -------------------------------
      # 5️⃣ Upload built JAR to AWS EC2
      # -------------------------------
      - name: Upload JAR to EC2
        uses: appleboy/scp-action@v0.1.7
        with:
          host: ${{ vars.EC2_HOST }}
          username: ubuntu
          key: ${{ secrets.EC2_KEY }}
          # 👇 Update this path if your JAR is under a subfolder like APP_name/build/libs/
          source: "build/libs/*.jar"
          target: "/home/ubuntu/app/"

      # -------------------------------
      # 6️⃣ Restart Spring Boot service via systemd
      # -------------------------------
      - name: Restart Spring Boot service on EC2
        uses: appleboy/ssh-action@v1.2.0
        with:
          host: ${{ vars.EC2_HOST }}
          username: ubuntu
          key: ${{ secrets.EC2_KEY }}
          script_stop: false
          script: |
            set -e
      
            echo "🔹 Deploying latest build to EC2..."
            mkdir -p /home/ubuntu/app/build/libs
            cd /home/ubuntu/app/build/libs || { echo "❌ Directory not found!"; exit 1; }
      
            echo "📄 Listing JAR files before cleanup..."
            ls -lh *.jar || echo "⚠️ No JAR files found yet."
      
            echo "🧾 Detecting latest runnable JAR..."
            # ✅ Pick only executable (non-plain) JARs
            LATEST_JAR=$(ls -t *-SNAPSHOT.jar 2>/dev/null | grep -v "plain" | head -n 1 || true)
            if [ -z "$LATEST_JAR" ]; then
              echo "❌ No valid (non-plain) JAR found! Exiting..."
              exit 1
            fi
      
            echo "✅ Found JAR to deploy: $LATEST_JAR"
      
            echo "🧹 Cleaning old JARs (keeping latest)..."
            find . -type f -name "*.jar" ! -newer "$LATEST_JAR" -delete || true
      
            echo "🆕 Copying $LATEST_JAR to /home/ubuntu/app/app.jar"
            sudo cp "$LATEST_JAR" /home/ubuntu/app/app.jar
      
            echo "💀 Restarting Spring Boot service..."
            sudo systemctl daemon-reload
            sudo systemctl stop springboot || true
            sleep 2
            sudo systemctl start springboot
      
            echo "✅ Checking service status..."
            sudo systemctl status springboot --no-pager || true
      
            echo "🎯 Deployment complete!"
      
